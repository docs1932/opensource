
# 1.可以安装最新的 powershell 脚本
https://github.com/PowerShell/PowerShell/releases

# 2.实用操作
## 2.1 自动补全
```
有时候输入一个命令时，输入的命令可能过长，这时可以使用 Tab 键进行自动补全。
1. 输入命令时，按 Tab 键，如果命令存在，则自动补全。
2.但是有时候，直接按 Tab 键，会补全一个错误的提示，不是第一个匹配的提示，这时可以按下 -> 箭头选中该提示，然后回车即可 。
3. 如果没有匹配的提示，则也可以一直按下 Tab 键，直到匹配到正确的提示。
```